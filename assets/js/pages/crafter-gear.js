const ids=['gCraft','gCraftReq','gControl','gControlReq','gCp','gCpReq'];function n(id){return Math.max(0,+document.getElementById(id).value||0)}function save(){const o={};ids.forEach(id=>o[id]=n(id));FF14Tools.save('crafterGearJudge',o)}gearJudge.onclick=()=>{const vals=[['作業精度',n('gCraft'),n('gCraftReq')],['加工精度',n('gControl'),n('gControlReq')],['CP',n('gCp'),n('gCpReq')]],lack=vals.map(x=>[x[0],Math.max(0,x[2]-x[1]),x[1],x[2]]).filter(x=>x[1]>0);if(!lack.length){gearResult.innerHTML='<h3>更新を急ぐ必要はありません</h3><p>入力した必要値はすべて満たしています。作りたいレシピが成立するなら、次の更新まで現装備を使えます。</p>'}else{const max=lack.slice().sort((a,b)=>b[1]-a[1])[0];gearResult.innerHTML='<h3>不足している項目</h3><div class="result-list">'+lack.map(x=>`<div class="result-item"><strong>${x[0]}：${x[1]}不足</strong><span>現在 ${x[2]} / 目標 ${x[3]}</span></div>`).join('')+`</div><div class="callout">まず「${max[0]}」の不足を埋める更新を優先してください。少量ならマテリア調整、大きい不足なら主道具・胴などステータス量の大きい部位から確認すると効率的です。</div>`}save()};const st=FF14Tools.load('crafterGearJudge',null);if(st)ids.forEach(id=>{if(st[id]!=null)document.getElementById(id).value=st[id]});